# program that uses myfunctions.py module to prompt a user for a number and return the list of the Fibonacci sequence

import myfunctions 

nTimes = int(input('how many:')) 
print(myfunctions.fibonacci(nTimes)) 